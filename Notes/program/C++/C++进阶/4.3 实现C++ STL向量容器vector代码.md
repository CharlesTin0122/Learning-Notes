```cpp
#include <cstdlib>
#include <iostream>
#include <stdexcept>

// 实现vector类
template <typename T>
class MyVector
{
public:
    // 构造函数
    MyVector(int size = 10)
        : _first(new T[size]), _last(_first), _end(_first + size)
    {
    }
    // 拷贝构造函数
    MyVector(const MyVector<T> &other)
    {
        // 计算参数对象长度
        int otherSize = other._end - other._first;
        // 分配新内存
        _first = new T[otherSize];
        // 拷贝数据
        int otherLen = other._last - other._first;
        for (int i = 0; i < otherLen; i++)
        {
            this->_first[i] = other._first[i];
        }

        this->_last = this->_first + otherLen;
        this->_end = this->_first + otherSize;
    }
    // 赋值运算符重载
    MyVector<T> &operator=(const MyVector<T> &other)
    {
        // 防止自赋值
        if (this == &other) return *this;
        // 删除旧数据
        delete[] _first;
        // 填充新数据
        int otherSize = other._end - other._first;
        _first = new T[otherSize];
        int otherLen = other._last - other._first;
        for (int i = 0; i < otherLen; i++)
        {
            this->_first[i] = other._first[i];
        }

        this->_last = this->_first + otherLen;
        this->_end = this->_first + otherSize;

        return *this;
    }
    ~MyVector()
    {
        delete[] _first;
        _first = _last = _end = nullptr;
    }
    // 添加元素
    void push(const T &val)
    {
        if (this->full()) this->expand();
        //_last指针解引用赋值val,然后_last自增
        *_last++ = val;
    }
    // 弹出元素
    void pop()
    {
        if (this->empty()) throw std::runtime_error("vector is empty!");
        --_last;
    }
    // 返回末尾元素
    T back() const
    {
        if (this->empty()) throw std::runtime_error("vector is empty!");
        return *(_last - 1);
    }
    bool full() const { return _last == _end; }
    bool empty() const { return _last == _first; }
    int size() const { return _last - _first; }

    void print() const
    {
        for (int i = 0; i < this->size(); i++)
        {
            std::cout << _first[i] << " ";
        }
        std::cout << std::endl;
    }

private:
    T *_first; // 指向数组起始位置
    T *_last;  // 指向数组中有效元素的后继位置
    T *_end;   // 指向数组空间的后继位置
    // 扩容
    void expand()
    {
        // 获取新数组容量和长度
        int newSize = (_end - _first) * 2;
        int len = _last - _first;
        // 申请新空间
        T *newData = new T[newSize];
        // 赋值
        for (int i = 0; i < len; i++)
        {
            newData[i] = _first[i];
        }
        delete[] _first;
        _first = newData;
        _last = _first + len;
        _end = _first + newSize;
    }
};

int main()
{
    MyVector<int> vec;
    for (int i = 0; i < 20; i++)
    {
        vec.push(rand() % 100);
    }
    vec.print();
    vec.pop();
    vec.print();
    std::cout << std::endl;
}
```
- 实现的这个vector数据结构还缺少空间配置器allocator