我们来看一个命令
```python
mc.polyCube("myCube", q=True, sx=True)
```
- 这条命令参数中：
	- "myCube"是一个字符串参数，我们称之为命令对象（Command Object），这通常是一个对象的名称，也叫做命令参数（Command Argument）
- 后面的参数是Flag（标志），q和sx都是短名称，True都是标志的值，我们称之为标志参数（Flag Argument）
---

### 1. **自定义命令的基本概念**
- **MPxCommand**：`OpenMayaMPx.MPxCommand` 是Maya Python API中用于创建自定义命令的基类。开发者需要继承这个类并实现核心方法（如 `doIt`）来定义命令的行为。
- **参数支持**：通过实现 `isUndoable` 和 `undoIt`/`redoIt` 方法，可以支持命令的撤销和重做功能。
- **参数解析**：自定义命令可以通过 `MSyntax` 和 `MArgDatabase` 类定义和解析参数（标志），支持短标志（如 `-n`）、长标志（如 `-name`）以及带值的参数。

#### 1.**MSyntax 介绍**

##### **定义**

MSyntax 是 Maya API 中的一个类，位于 OpenMaya 模块，用于定义自定义命令的语法（即命令支持的标志和参数类型）。它指定了命令可以接受哪些标志（flags）、这些标志的类型（如字符串、整数、布尔等），以及标志的行为（例如是否允许多个值）。

##### **主要功能**

- **定义标志**：通过 addFlag 方法为命令添加短标志（如 -n）和长标志（如 -name），并指定其数据类型。
- **支持多种参数类型**：包括布尔、整数、浮点数、字符串、选择对象等。
- **支持对象列表**：通过 setObjectType 定义命令接受的对象（如选择列表中的节点）。
- **查询/编辑模式**：通过 enableQuery 和 enableEdit 支持命令的查询（-query）和编辑（-edit）模式。

##### **常用方法**

- addFlag(shortName, longName, argType)：添加一个标志，指定短名称、长名称和参数类型。
    - shortName：短标志（如 -n）。
    - longName：长标志（如 -name）。
    - argType：参数类型，如 MSyntax.kString、MSyntax.kLong、MSyntax.kBoolean 等。
- setObjectType(objectType, minObjects, maxObjects)：定义命令接受的对象类型和数量范围。
    - objectType：如 MSyntax.kSelectionItem（选择对象）。
    - minObjects / maxObjects：对象的最小和最大数量。
- useSelectionDefault(bool)：使用场景内选择的对象作为默认参数
- enableQuery(boolean)：启用或禁用查询模式（-query 或 -q）。
- enableEdit(boolean)：启用或禁用编辑模式（-edit 或 -e）。
- makeFlagMultiUse(flag)：允许标志被多次使用（例如，-n "name1" -n "name2"）。

##### **支持的参数类型**

- MSyntax.kBoolean：布尔值（如 -v true）。
- MSyntax.kLong：整数（如 -c 5）。
- MSyntax.kDouble：浮点数（如 -r 3.14）。
- MSyntax.kString：字符串（如 -n "example"）。
- MSyntax.kSelectionItem：选择对象（如节点或组件）。
- MSyntax.kNoArg：无参数的标志（仅标志存在与否）。

---

####  2.**MArgDatabase 介绍**

##### **定义**

MArgDatabase 是 Maya API 中的一个类，位于 OpenMaya 模块，用于解析用户在命令中传递的参数（基于 MSyntax 定义的语法）。它从 MArgList（用户输入的参数列表）中提取标志和值，并提供便捷的方法来访问这些参数。

MArgDatabase是MArgParser的子类，这个类有解析和储存标志参数的能力，继承后增加了检索功能。

##### **主要功能**

- **解析参数**：根据 MSyntax 定义的语法，从 MArgList 中提取标志的值。
- **检查标志存在性**：通过 isFlagSet 检查是否提供了某个标志。
- **获取参数值**：根据标志名称获取布尔、整数、浮点数、字符串等值。
- **支持查询/编辑模式**：检查命令是否以查询（-query）或编辑（-edit）模式运行。
- **处理对象列表**：获取命令中传递的对象（如选中的节点）。

##### **常用方法**

- isFlagSet(flag)：检查指定标志是否在参数列表中。
- flagArgumentBool(flag, index)：获取布尔标志的值。
- flagArgumentInt(flag, index)：获取整数标志的值。
- flagArgumentDouble(flag, index)：获取浮点数标志的值。
- flagArgumentString(flag, index)：获取字符串标志的值。
- getObjects(selectionList)：获取命令中传递的对象（存储到 MSelectionList）。
- isQuery()：检查是否为查询模式。
- isEdit()：检查是否为编辑模式。
- flagArgumentCount(flag)：返回某标志被使用的次数（用于多值标志）。
---
#### 3.Undo和Redo
- 在 Autodesk Maya 中，Undo 和 Redo 的数据结构基于栈（Stack），具体是一个双栈模型，即一个 Undo 栈和一个 Redo 栈。这种设计符合命令模式（Command Pattern）中对撤销和重做的典型实现方式。
- **Undo 栈**：
    - 每次执行一个命令（或操作），Maya 将该命令（通常以命令对象或相关数据结构的形式）压入 **Undo 栈**。
    - Undo 栈是一个后进先出（LIFO, Last-In-First-Out）的结构，意味着最近执行的命令会首先被撤销。
    - 当用户执行 Undo 操作（例如按 Ctrl+Z 或调用 cmds.undo()），Maya 从 Undo 栈顶部弹出一个命令，执行其 undoIt 方法（如果命令支持撤销），并将该命令压入 Redo 栈。
- **Redo 栈**：
    - Redo 栈同样是一个 LIFO 结构，用于存储已撤销的命令。
    - 当用户执行 Redo 操作（例如按 Ctrl+Shift+Z 或调用 cmds.redo()），Maya 从 Redo 栈顶部弹出一个命令，执行其 redoIt 方法，并将该命令重新压回 Undo 栈。
- **双栈模型**：
    - Undo 和 Redo 栈共同工作，形成一个双向的操作历史记录。
    - 每次执行新命令时，Redo 栈会被清空，因为新的操作会覆盖之前撤销的历史。
- [Open: Pasted image 20250728062330.png](Notes/MAYA/Maya%20Python%20API/attachments/63373e6eb7a72b258047366ee6d3dcd8_MD5.jpeg)
![](Notes/MAYA/Maya%20Python%20API/attachments/63373e6eb7a72b258047366ee6d3dcd8_MD5.jpeg)
- 当我们执行创建平面，Undo队列也会写入一个创建平面的命令
- 当我们移动平面，Undo队列也会写入一个移动平面的命令
- 当我们旋转平面，Undo队列也会写入一个旋转平面的命令
- 当我们执行Undo撤销旋转命令，Redo队列会写入一个旋转命令
- 当我们再次执行Undo撤销位移命令，Redo队列会写入一个位移命令
### 2. **实现自定义命令的步骤**
以下是创建带参数的自定义命令的完整流程：

#### **步骤 1：导入必要的模块**
需要使用 `OpenMaya` 和 `OpenMayaMPx` 模块来定义命令和处理参数。

```python
import maya.OpenMaya as om
import maya.OpenMayaMPx as ompx
```

#### **步骤 2：定义命令类**
继承 `ompx.MPxCommand` 类，并实现以下关键方法：
- `doIt(argList)`：命令的主要执行逻辑，接收参数列表（`MArgList`）。
- `isUndoable()`：返回布尔值，指示命令是否支持撤销。
- `undoIt()` 和 `redoIt()`：实现撤销和重做逻辑（如果支持撤销）。
- `creator()`：静态方法，返回命令实例。
- `syntaxCreator()`：定义命令的语法（标志和参数）。

#### **步骤 3：定义命令语法**
使用 `MSyntax` 类定义命令支持的标志和参数。常见的标志类型包括：
- 布尔标志（如 `-enable`）
- 字符串参数（如 `-name "value"`)
- 数字参数（如 `-count 5`）

#### **步骤 4：解析参数**
在 `doIt` 方法中使用 `MArgDatabase` 解析传入的参数，并根据参数值执行逻辑。

#### **步骤 5：注册命令**
通过插件注册函数将命令注册到Maya，使其可以通过MEL或Python调用。

---

### 3. **示例：带参数的自定义命令**
以下是一个完整的示例，展示如何创建一个带参数的自定义命令 `customPrintCmd`，该命令接受以下参数：
- `-n` / `-name`（字符串）：指定一个名称。
- `-c` / `-count`（整数）：指定打印次数。
- `-v` / `-verbose`（布尔）：控制是否打印详细信息。

```python
import maya.OpenMaya as om
import maya.OpenMayaMPx as ompx

# 命令名称
kPluginCmdName = "customPrintCmd"

# 标志定义
kNameFlag = "-n"
kNameLongFlag = "-name"
kCountFlag = "-c"
kCountLongFlag = "-count"
kVerboseFlag = "-v"
kVerboseLongFlag = "-verbose"

class CustomPrintCmd(ompx.MPxCommand):
    def __init__(self):
        ompx.MPxCommand.__init__(self)
        self.name = ""
        self.count = 1
        self.verbose = False

    @staticmethod
    def creator():
        """创建命令实例"""
        return ompx.asMPxPtr(CustomPrintCmd())

    @staticmethod
    def syntaxCreator():
        """定义命令语法"""
        syntax = om.MSyntax()
        # 添加字符串类型的 -name 标志
        syntax.addFlag(kNameFlag, kNameLongFlag, om.MSyntax.kString)
        # 添加整数类型的 -count 标志
        syntax.addFlag(kCountFlag, kCountLongFlag, om.MSyntax.kLong)
        # 添加布尔类型的 -verbose 标志
        syntax.addFlag(kVerboseFlag, kVerboseLongFlag, om.MSyntax.kBoolean)
        return syntax

    def isUndoable(self):
        """支持撤销"""
        return True

    def undoIt(self):
        """撤销逻辑：打印撤销信息"""
        om.MGlobal.displayInfo(f"Undoing command for name: {self.name}")

    def redoIt(self):
        """重做逻辑：重新执行打印"""
        self.executeCommand()

    def doIt(self, argList):
        """主执行逻辑：解析参数并调用执行"""
        # 创建参数解析器
        argData = om.MArgDatabase(self.syntax(), argList)

        # 解析参数
        if argData.isFlagSet(kNameFlag):
            self.name = argData.flagArgumentString(kNameFlag, 0)
        if argData.isFlagSet(kCountFlag):
            self.count = argData.flagArgumentInt(kCountFlag, 0)
        if argData.isFlagSet(kVerboseFlag):
            self.verbose = argData.flagArgumentBool(kVerboseFlag, 0)

        # 执行命令逻辑
        self.executeCommand()

        # 设置结果
        self.setResult(f"Printed {self.name} {self.count} times")

    def executeCommand(self):
        """实际执行打印逻辑"""
        for i in range(self.count):
            message = f"Print {i+1}: {self.name}"
            if self.verbose:
                message += " (Verbose mode)"
            om.MGlobal.displayInfo(message)

# 插件初始化
def maya_useNewAPI():
    pass

def initializePlugin(mobject):
    """注册插件"""
    mplugin = ompx.MFnPlugin(mobject, "Custom Author", "1.0", "Any")
    try:
        mplugin.registerCommand(kPluginCmdName, CustomPrintCmd.creator, CustomPrintCmd.syntaxCreator)
    except:
        om.MGlobal.displayError("Failed to register command: %s" % kPluginCmdName)
        raise

def uninitializePlugin(mobject):
    """卸载插件"""
    mplugin = ompx.MFnPlugin(mobject)
    try:
        mplugin.deregisterCommand(kPluginCmdName)
    except:
        om.MGlobal.displayError("Failed to deregister command: %s" % kPluginCmdName)
        raise
```

---

### 4. **代码说明**
- **命令名称**：`customPrintCmd`，可以通过MEL或Python调用（如 `customPrintCmd -n "Test" -c 3 -v true`）。
- **标志定义**：
  - `-n` / `-name`：接受字符串参数，指定要打印的名称。
  - `-c` / `-count`：接受整数参数，指定打印次数。
  - `-v` / `-verbose`：布尔标志，控制是否打印详细信息。
- **语法定义**：`syntaxCreator` 方法使用 `MSyntax` 添加标志，指定每个标志的类型（字符串、整数、布尔）。
- **参数解析**：`doIt` 方法使用 `MArgDatabase` 解析传入的 `MArgList`，提取标志的值。
- **撤销/重做**：通过 `isUndoable`、`undoIt` 和 `redoIt` 支持撤销功能。
- **插件注册**：`initializePlugin` 和 `uninitializePlugin` 用于注册和卸载命令。

---

### 5. **如何使用**
1. **保存代码**：将上述代码保存为 `customPrintCmd.py`。
2. **加载插件**：在Maya的脚本编辑器中运行以下代码加载插件：
   ```python
   import maya.cmds as cmds
   cmds.loadPlugin("customPrintCmd.py")
   ```
3. **执行命令**：在Maya的命令行或脚本编辑器中运行：
   ```python
   # 示例 1：基本调用
   cmds.customPrintCmd(name="Hello", count=2)

   # 示例 2：使用短标志和详细模式
   cmds.customPrintCmd(n="World", c=3, v=True)
   ```
   输出示例：
   ```
   Print 1: World
   Print 2: World
   Print 3: World (Verbose mode)
   Result: Printed World 3 times
   ```

4. **撤销命令**：在Maya中按 `Ctrl+Z`（或运行 `cmds.undo()`）将触发 `undoIt` 方法。

---

### 6. **关键点和注意事项**
- **标志类型**：`MSyntax` 支持多种参数类型：
  - `kString`：字符串。
  - `kLong` / `kDouble`：整数/浮点数。
  - `kBoolean`：布尔值。
  - `kSelectionItem`：选择对象。
- **错误处理**：在 `doIt` 中检查 `argData.isFlagSet` 避免未提供参数时的错误。
- **性能**：对于复杂命令，避免在 `doIt` 中执行所有逻辑，建议将核心逻辑放入 `redoIt`，以优化撤销/重做性能。
- **插件管理**：确保插件正确注册和卸载，避免冲突。
- **命名冲突**：命令名称和标志名称应避免与Maya内置命令冲突。

---

### 7. **扩展功能**
如果需要更复杂的参数处理，可以：
- **支持多值参数**：通过 `MSyntax.setObjectType` 定义多对象参数（如选择列表）。
- **添加查询模式**：通过 `argData.isQuery()` 支持查询功能。
- **添加编辑模式**：通过 `argData.isEdit()` 支持修改现有对象。

示例（支持查询模式）：
```python
def doIt(self, argList):
    argData = om.MArgDatabase(self.syntax(), argList)
    if argData.isQuery():
        self.setResult(f"Name: {self.name}, Count: {self.count}, Verbose: {self.verbose}")
        return
    # 正常执行逻辑...
```

---

### 8. **总结**
通过继承 `OpenMayaMPx.MPxCommand` 并使用 `MSyntax` 和 `MArgDatabase`，可以在Maya Python API 1.0中创建带参数的自定义命令。上述示例展示了如何定义一个支持字符串、整数和布尔参数的命令，并实现撤销/重做功能。开发者可以根据需求扩展标志类型、添加查询/编辑模式，或集成更复杂的逻辑。
# 案例
- 为选中Mesh的每个顶点创建一个粒子
```python
import maya.OpenMaya as om
import maya.OpenMayaMPx as mpx
import maya.OpenMayaFX as fx

# --- 常量定义 ---

# 命令名称
kPluginCommandName = "vertexParticle"
# 标志
kHelpFlag = "-h"
kHelpLongFlag = "-help"
kSparseFlag = "-s"
kSparseLongFlag = "-sparse"
# 帮助文本
helpMessage = "This command creates a particle system from the vertices of a selected mesh. Use the -s or -sparse flag to specify the interval of vertices to emit from. Example: vertexParticle -s 5;"


# ==============================================================================
# 自定义命令类
# ==============================================================================
class PluginCommand(mpx.MPxCommand):
    """
    自定义 Maya 命令插件，用于从网格顶点创建粒子系统。
    """

    def __init__(self):
        """
        构造函数：初始化父类和实例变量。
        """
        mpx.MPxCommand.__init__(self)
        # 为 sparse 设置一个安全的默认值（为每个顶点创建粒子），避免在未提供标志时出错。
        self.sparse = 1
        # 用于存储创建的粒子对象，以便未来可能实现的撤销操作
        self.mObj_Particles = None

    @staticmethod
    def cmdCreator():
        """创建命令实例并返回其指针"""
        return mpx.asMPxPtr(PluginCommand())

    @staticmethod
    def syntaxCreator():
        """定义命令的语法（标志和参数）"""
        syntax = om.MSyntax()
        # 添加帮助标志
        syntax.addFlag(kHelpFlag, kHelpLongFlag)
        # 添加稀疏度标志，它需要一个整数参数。kDouble 也可以，但 kLong (integer) 更精确
        syntax.addFlag(kSparseFlag, kSparseLongFlag, om.MSyntax.kLong)
        return syntax

    def doIt(self, argList):
        """
        命令的主要执行方法。
        负责参数解析、流程控制和调用核心功能。
        """
        # 1. 解析参数
        # 获取实例的语法对象
        # OpenMayaMPx.MPxCommand 基类提供了一个内置方法 syntax()，
        # 它返回由 syntaxCreator 方法创建的 MSyntax 对象。
        try:
            argData = om.MArgDatabase(self.syntax(), argList)
        except RuntimeError:
            om.MGlobal.displayError("Invalid arguments.")
            return

        # 2. 处理帮助标志
        # setResult()函数是一个用于设置命令输出结果的方法，属于 OpenMayaMPx.MPxCommand 类的功能
        if argData.isFlagSet(kHelpFlag) or argData.isFlagSet(kHelpLongFlag):
            self.setResult(helpMessage)
            return  # 显示帮助信息后直接结束

        # 3. 处理稀疏度标志
        if argData.isFlagSet(kSparseFlag) or argData.isFlagSet(kSparseLongFlag):
            # isFlagSet 只检查标志是否存在，我们需要确定是哪个标志来获取其值
            flag_to_use = kSparseFlag
            if argData.isFlagSet(kSparseLongFlag):
                flag_to_use = kSparseLongFlag

            # 获取标志的参数值
            # getFlagArgument 返回一个MArgList, 我们需要获取其中的具体类型
            self.sparse = argData.flagArgumentInt(
                flag_to_use, 0
            )  # 使用 Int 更符合间隔的语义

            # 增加对无效值的检查
            if self.sparse <= 0:
                om.MGlobal.displayError("Sparse value must be a positive integer.")
                return

        # 4. 调用核心逻辑
        # Maya 的撤销队列会调用 redoIt，所以我们将核心逻辑放在 redoIt 中
        self.redoIt()

    def redoIt(self):
        """
        核心功能实现：在网格顶点上创建粒子。
        这个方法会被 doIt 首次调用，也会在执行“重做”操作时被 Maya 调用。
        """
        # --- 1. 获取选中的网格 ---
        selection = om.MSelectionList()
        om.MGlobal.getActiveSelectionList(selection)

        if selection.isEmpty():
            om.MGlobal.displayError("Error: Please select a polygon mesh.\n")
            return

        dagPath = om.MDagPath()
        try:
            selection.getDagPath(0, dagPath)
        except Exception:
            om.MGlobal.displayError("Error: Could not get DAG path from selection.\n")
            return

        # 检查选中的是否是 mesh
        if not dagPath.hasFn(om.MFn.kMesh):
            om.MGlobal.displayError(
                "Error: The selected object is not a polygon mesh.\n"
            )
            return

        mFnMesh = om.MFnMesh(dagPath)

        # --- 2. 获取顶点位置 ---
        pointArray = om.MPointArray()
        mFnMesh.getPoints(pointArray, om.MSpace.kWorld)

        if pointArray.length() == 0:
            om.MGlobal.displayError("Error: The selected mesh has no vertices.\n")
            return

        # --- 3. 创建并操作粒子系统 ---
        try:
            # 创建粒子系统函数集
            mFnParticle = fx.MFnParticleSystem()
            # 创建粒子塑形节点和变换节点，返回塑形节点的 MObject
            self.mObj_Particles = mFnParticle.create()

            # 【关键修复】必须用新创建的粒子 MObject 重新初始化函数集
            mFnParticle.setObject(self.mObj_Particles)

            # --- 4. 遍历顶点并发射粒子 ---
            counter = 0
            for i in range(pointArray.length()):
                if i % self.sparse == 0:
                    mFnParticle.emit(pointArray[i])
                    counter += 1

            # 打印结果信息
            om.MGlobal.displayInfo(f"Successfully created {counter} particles.")

            # 保存粒子初始状态，这样在回到第一帧时粒子会显示在正确位置
            mFnParticle.saveInitialState()

        except Exception as e:
            om.MGlobal.displayError(
                f"An error occurred during particle creation: {e}\n"
            )
            return

    def isUndoable(self):
        """
        如果需要让命令可撤销，则返回 True。
        你需要实现一个 undoIt 方法来删除已创建的粒子。
        """
        return True

    def undoIt(self):
        print("undo")
        # 创建DagModifier，处理Dag节点
        # DagModifier是一个栈，往里面添加修改操作，mDagModi.doIt()会执行所有操作
        mDagModi = om.MDagModifier()
        # 创建dagNode类，用于获取粒子系统的DagNode节点
        mFnDagNode = om.MFnDagNode(self.mObj_Particles)
        try:
            # 删除粒子系统第一个父层级的transform节点，会将粒子系统一起删除
            mDagModi.deleteNode(mFnDagNode.parent(0))
            mDagModi.doIt()
        except Exception as e:
            om.MGlobal.displayError(f"{e}")
        return

# 插件注册和卸载

def initializePlugin(mobject):
    """
    注册插件到 Maya
    mobject参数是maya生成的句柄用来捕获命令实例的指针
    """
    mplugin = mpx.MFnPlugin(mobject, "CharlesT", "1.1", "Any")
    try:
        mplugin.registerCommand(
            kPluginCommandName, PluginCommand.cmdCreator, PluginCommand.syntaxCreator
        )
    except Exception:
        om.MGlobal.displayError(f"Failed to register command: {kPluginCommandName}\n")
        raise


def uninitializePlugin(mobject):
    """
    从 Maya 卸载插件
    卸载插件只需命令名称即可
    """
    mplugin = mpx.MFnPlugin(mobject)
    try:
        mplugin.deregisterCommand(kPluginCommandName)
    except Exception:
        om.MGlobal.displayError(f"Failed to deregister command: {kPluginCommandName}\n")
        raise

```
- 命令使用方法
```python
import maya.cmds as mc
mc.vertexParticle(sparse=2)
```