在 Autodesk Maya 的 Python API 2.0 中，`MFnAnimCurve` 是 `OpenMayaAnim` 模块中的一个功能集类（Function Set），专门用于操作 Maya 场景中的动画曲线（Animation Curve）。它提供了对动画曲线的全面控制，允许开发者以编程方式查询和修改关键帧、切线、权重等属性。与 `MAnimCurve` 不同，`MFnAnimCurve` 是功能集类，设计用于直接操作动画曲线节点的属性和行为，属于 Maya 的 C++ API 的 Python 封装，提供了更高级和更易用的接口。

以下是对 `MFnAnimCurve` 的详细介绍，涵盖其功能、方法、使用场景以及与 `MAnimCurve` 的区别。

---

## 1. **概述**
`MFnAnimCurve` 是 Maya Python API 2.0 中用于处理动画曲线的功能集类，位于 `OpenMayaAnim` 模块。它通过绑定到一个表示动画曲线的 `MObject` 或 `MPlug` 来操作动画曲线节点（如 `animCurveTL`, `animCurveTA`, `animCurveTU` 等）。`MFnAnimCurve` 提供了直观的接口来管理动画曲线的关键帧、切线类型、无限延伸行为等，适合需要精确控制动画数据的开发任务。

与 `maya.cmds` 提供的命令（如 `setKeyframe` 或 `keyframe`）相比，`MFnAnimCurve` 是更底层的 API，性能更高，适合复杂的动画操作或自定义工具开发。

---

## 2. **主要功能**
MFnAnimCurve 是 Maya API 中的功能集（Function Set），用于处理动画曲线节点。它可以操作关键帧、切线、权重、时间等属性，支持对动画曲线的各种操作，如添加关键帧、修改切线类型、查询曲线值等。以下是 MFnAnimCurve 类的所有方法的详细说明，按功能分组，方便理解。

### **1. 构造函数和初始化**
- **`__init__(self)`**
  - 构造函数，创建一个空的 `MFnAnimCurve` 对象。
  - 示例：`fnCurve = om.MFnAnimCurve()`
  - 注意：需要后续通过 `create` 方法关联到实际的动画曲线节点。

- **`create(self, plug, animCurveType=None, status=None)`**
  - 创建一个新的动画曲线并关联到指定的 `MPlug` 或作为独立节点。
  - 参数：
    - `plug`（`MPlug`）：要连接到的属性（例如，对象的平移或旋转属性）。
    - `animCurveType`（`int`）：动画曲线类型（可选）。可选值包括：
      - `MFnAnimCurve.kAnimCurveTA`（时间到角度）
      - `MFnAnimCurve.kAnimCurveTL`（时间到线性）
      - `MFnAnimCurve.kAnimCurveTT`（时间到时间）
      - `MFnAnimCurve.kAnimCurveTU`（时间到单位）
      - `MFnAnimCurve.kAnimCurveUA`（单位到角度）
      - `MFnAnimCurve.kAnimCurveUL`（单位到线性）
      - `MFnAnimCurve.kAnimCurveUT`（单位到时间）
      - `MFnAnimCurve.kAnimCurveUU`（单位到单位）
    - `status`（`MStatus`）：返回操作的状态（成功或失败）。
  - 返回：`MObject`（新创建的动画曲线节点）。
  - 示例：
    ```python
    import maya.OpenMaya as om
    sel = om.MSelectionList()
    sel.add("pCube1.translateX")
    plug = om.MPlug()
    sel.getPlug(0, plug)
    fnCurve = om.MFnAnimCurve()
    curveObj = fnCurve.create(plug)
    ```

- **`setObject(self, object)`**
  - 将 `MFnAnimCurve` 绑定到指定的动画曲线节点（`MObject`）。
  - 参数：
    - `object`（`MObject`）：动画曲线节点。
  - 示例：
    ```python
    fnCurve.setObject(curveObj)
    ```

---

### **2. 查询动画曲线信息**
- **`animCurveType(self)`**
  - 返回动画曲线的类型（`MFnAnimCurve.AnimCurveType`）。
  - 返回值：整数，表示曲线类型（如 `kAnimCurveTL` 等）。
  - 示例：
    ```python
    curveType = fnCurve.animCurveType()
    print(curveType)  # 例如，1 表示 kAnimCurveTL
    ```

- **`numKeys(self)`**
  - 返回动画曲线的关键帧数量。
  - 返回值：`int`。
  - 示例：
    ```python
    num = fnCurve.numKeys()
    print(f"Number of keys: {num}")
    ```

- **`input(self, index)`**
  - 获取指定关键帧的时间。
  - 参数：
    - `index`（`int`）：关键帧索引（从 0 开始）。
  - 返回值：`MTime`（时间对象）。
  - 示例：
    ```python
    time = fnCurve.time(0)  # 获取第一个关键帧的时间
    print(time.value())
    ```

- **`value(self, index)`**
  - 获取指定关键帧的值。
  - 参数：
    - `index`（`int`）：关键帧索引。
  - 返回值：`float`（关键帧的值）。
  - 示例：
    ```python
    val = fnCurve.value(0)
    print(f"Value at key 0: {val}")
    ```

- **`evaluate(self, time, value)`**
  - 在指定时间点计算动画曲线的插值结果。
  - 参数：
    - `time`（`MTime`）：要计算的时间。
    - `value`（`float`）：输出参数，存储计算出的值。
  - 返回：`bool`（成功与否）。
  - 示例：
    ```python
    time = om.MTime(10.0)
    value = om.MScriptUtil().asDoublePtr()
    fnCurve.evaluate(time, value)
    print(om.MScriptUtil.getDouble(value))
    ```

- **`find(self, time, tolerance=None)`**
  - 在曲线上查找在指定时间关键帧的索引，未找到返回None
  - 参数：
    - `time`（`MTime`）：要查找的时间。
    - `tolerance`（`MTime`）：时间容差（可选）。
  - 返回值：`int`（关键帧索引，如果未找到返回 None）。
  - 示例：
    ```python
    index = fnCurve.find(om.MTime(5.0))
    print(f"Key index: {index}")
    ```

- **`findClosest(self, time)`**
  - 查找与指定时间最接近的关键帧索引（忽略容差）。
  - 参数：
    - `time`（`MTime`）：要查找的时间。
  - 返回值：`int`（关键帧索引）。
  - 示例：
    ```python
    index = fnCurve.findClosest(om.MTime(5.0))
    print(f"Closest key index: {index}")
    ```

---

### **3. 修改动画曲线**
- **`addKey(self, time, value, tangentInType=None, tangentOutType=None, change=None)`**
  - 在指定时间添加一个关键帧。
  - 参数：
    - `time`（`MTime`）：关键帧时间。
    - `value`（`float`）：关键帧值。
    - `tangentInType`（`int`）：输入切线类型（可选）。可选值包括：
      - `MFnAnimCurve.kTangentGlobal`
      - `MFnAnimCurve.kTangentFixed`
      - `MFnAnimCurve.kTangentLinear`
      - `MFnAnimCurve.kTangentFlat`
      - `MFnAnimCurve.kTangentSmooth`
      - `MFnAnimCurve.kTangentStep`
      - `MFnAnimCurve.kTangentClamped`
    - `tangentOutType`（`int`）：输出切线类型（可选）。
    - `change`（`MAnimCurveChange`）：用于撤销/重做的更改缓存（可选）。
  - 返回：`int`（新关键帧的索引）。
  - 示例：
    ```python
    index = fnCurve.addKey(om.MTime(5.0), 10.0, om.MFnAnimCurve.kTangentLinear)
    print(f"New key index: {index}")
    ```
- `insertKey(time, breakdown=False, change=None) -> int`
	- 在指定时间插入帧，保持曲线形状

- **`addKeys(self, timeArray, valueArray, tangentInType=None, tangentOutType=None, keepExistingKeys=None, change=None)`**
  - 批量添加多个关键帧。
  - 参数：
    - `timeArray`（`MTimeArray`）：时间数组。
    - `valueArray`（`MDoubleArray`）：值数组。
    - `tangentInType`（`int`）：输入切线类型（可选）。
    - `tangentOutType`（`int`）：输出切线类型（可选）。
    - `keepExistingKeys`（`bool`）：是否保留现有关键帧（默认 False）。
    - `change`（`MAnimCurveChange`）：更改缓存（可选）。
  - 返回：`int`（添加的关键帧数量）。
  - 示例：
    ```python
    times = om.MTimeArray()
    values = om.MDoubleArray()
    times.append(om.MTime(1.0))
    values.append(2.0)
    fnCurve.addKeys(times, values)
    ```

- **`remove(self, index, change=None)`**
  - 删除指定索引的关键帧。
  - 参数：
    - `index`（`int`）：关键帧索引。
    - `change`（`MAnimCurveChange`）：更改缓存（可选）。
  - 示例：
    ```python
    fnCurve.remove(0)
    ```

- **`setValue(self, index, value, change=None)`**
  - 设置指定关键帧的值。
  - 参数：
    - `index`（`int`）：关键帧索引。
    - `value`（`float`）：新值。
    - `change`（`MAnimCurveChange`）：更改缓存（可选）。
  - 示例：
    ```python
    fnCurve.setValue(0, 15.0)
    ```

- **`setInput(self, index, time, change=None)`**
  - 设置指定关键帧的时间。
  - 参数：
    - `index`（`int`）：关键帧索引。
    - `time`（`MTime`）：新时间。
    - `change`（`MAnimCurveChange`）：更改缓存（可选）。
  - 示例：
    ```python
    fnCurve.setTime(0, om.MTime(2.0))
    ```

- **`setTangent(self, index, isInTangent, x, y, change=None)`**
  - 设置指定关键帧的输入或输出切线。
  - 参数：
    - `index`（`int`）：关键帧索引。
    - `isInTangent`（`bool`）：True 表示输入切线，False 表示输出切线。
    - `x`（`float`）：切线的 x 分量（时间方向）。
    - `y`（`float`）：切线的 y 分量（值方向）。
    - `change`（`MAnimCurveChange`）：更改缓存（可选）。
  - 示例：
    ```python
    fnCurve.setTangent(0, True, 1.0, 0.5)
    ```

- **`setInTangentType(self, index, tangentType, change=None)`**
  - 设置指定关键帧的输入切线类型。
  - 参数：
    - `index`（`int`）：关键帧索引。
    - `tangentType`（`int`）：切线类型（如 `kTangentLinear`）。
    - `change`（`MAnimCurveChange`）：更改缓存（可选）。
  - 示例：
    ```python
    fnCurve.setInTangentType(0, om.MFnAnimCurve.kTangentLinear)
    ```

- **`setOutTangentType(self, index, tangentType, change=None)`**
  - 设置指定关键帧的输出切线类型。
  - 参数同上。
  - 示例：
    ```python
    fnCurve.setOutTangentType(0, om.MFnAnimCurve.kTangentSmooth)
    ```

- **`setIsBreakdown(self, index, isBreakdown, change=None)`**
  - 设置指定关键帧是否为“Breakdown”关键帧（用于动画层级）。
  - 参数：
    - `index`（`int`）：关键帧索引。
    - `isBreakdown`（`bool`）：是否为 Breakdown。
    - `change`（`MAnimCurveChange`）：更改缓存（可选）。
  - 示例：
    ```python
    fnCurve.setIsBreakdown(0, True)
    ```

- **`setIsWeighted(self, isWeighted)`**
  - 设置动画曲线是否使用加权切线。
  - 参数：
    - `isWeighted`（`bool`）：True 表示启用加权切线。
  - 示例：
    ```python
    fnCurve.setIsWeighted(True)
    ```

- **`setPreInfinityType(self, infinityType)`**
  - 设置曲线前无限延伸的行为。
  - 参数：
    - `infinityType`（`int`）：前无限类型，可选值：
      - `MFnAnimCurve.kConstant`
      - `MFnAnimCurve.kLinear`
      - `MFnAnimCurve.kCycle`
      - `MFnAnimCurve.kCycleRelative`
      - `MFnAnimCurve.kOscillate`
  - 示例：
    ```python
    fnCurve.setPreInfinityType(om.MFnAnimCurve.kCycle)
    ```

- **`setPostInfinityType(self, infinityType)`**
  - 设置曲线后无限延伸的行为。
  - 参数同上。
  - 示例：
    ```python
    fnCurve.setPostInfinityType(om.MFnAnimCurve.kConstant)
    ```

---

### **4. 切线和权重相关**
- **`getTangent(self, index, isInTangent, x, y)`**
  - 获取指定关键帧的输入或输出切线值。
  - 参数：
    - `index`（`int`）：关键帧索引。
    - `isInTangent`（`bool`）：True 表示输入切线，False 表示输出切线。
    - `x`（`float`）：输出参数，切线的 x 分量。
    - `y`（`float`）：输出参数，切线的 y 分量。
  - 返回：`bool`（成功与否）。
  - 示例：
    ```python
    x = om.MScriptUtil().asDoublePtr()
    y = om.MScriptUtil().asDoublePtr()
    fnCurve.getTangent(0, True, x, y)
    print(om.MScriptUtil.getDouble(x), om.MScriptUtil.getDouble(y))
    ```

- **`getInTangentType(self, index)`**
  - 获取指定关键帧的输入切线类型。
  - 参数：
    - `index`（`int`）：关键帧索引。
  - 返回值：`int`（切线类型）。
  - 示例：
    ```python
    tangentType = fnCurve.getInTangentType(0)
    ```

- **`getOutTangentType(self, index)`**
  - 获取指定关键帧的输出切线类型。
  - 参数同上。
  - 返回值：`int`（切线类型）。
  - 示例：
    ```python
    tangentType = fnCurve.getOutTangentType(0)
    ```

- **`isWeighted(self)`**
  - 检查动画曲线是否使用加权切线。
  - 返回值：`bool`。
  - 示例：
    ```python
    weighted = fnCurve.isWeighted()
    print(f"Is weighted: {weighted}")
    ```

- **`setWeight(self, index, isInTangent, weight, change=None)`**
  - 设置指定关键帧的输入或输出切线权重。
  - 参数：
    - `index`（`int`）：关键帧索引。
    - `isInTangent`（`bool`）：True 表示输入切线，False 表示输出切线。
    - `weight`（`float`）：切线权重值。
    - `change`（`MAnimCurveChange`）：更改缓存（可选）。
  - 示例：
    ```python
    fnCurve.setWeight(0, True, 0.5)
    ```

- **`getWeight(self, index, isInTangent)`**
  - 获取指定关键帧的输入或输出切线权重。
  - 参数：
    - `index`（`int`）：关键帧索引。
    - `isInTangent`（`bool`）：True 表示输入切线，False 表示输出切线。
  - 返回值：`float`（权重值）。
  - 示例：
    ```python
    weight = fnCurve.getWeight(0, True)
    print(f"In tangent weight: {weight}")
    ```

---

### **5. 其他方法**
- **`isStatic(self, change=None)`**
  - 检查动画曲线是否为静态（即没有动画效果，所有值相同）。
  - 参数：
    - `change`（`MAnimCurveChange`）：更改缓存（可选）。
  - 返回值：`bool`。
  - 示例：
    ```python
    isStatic = fnCurve.isStatic()
    print(f"Is static: {isStatic}")
    ```

- **`isUnitlessInput(self)`**
  - 检查动画曲线的输入是否为无单位（例如，单位到角度曲线）。
  - 返回值：`bool`。
  - 示例：
    ```python
    unitless = fnCurve.isUnitlessInput()
    print(f"Unitless input: {unitless}")
    ```

- **`getPreInfinityType(self)`**
  - 获取曲线前无限延伸的类型。
  - 返回值：`int`（如 `kConstant`）。
  - 示例：
    ```python
    preType = fnCurve.getPreInfinityType()
    ```

- **`getPostInfinityType(self)`**
  - 获取曲线后无限延伸的类型。
  - 返回值：`int`。
  - 示例：
    ```python
    postType = fnCurve.getPostInfinityType()
    ```

- **`isBreakdown(self, index)`**
  - 检查指定关键帧是否为 Breakdown 关键帧。
  - 参数：
    - `index`（`int`）：关键帧索引。
  - 返回值：`bool`。
  - 示例：
    ```python
    isBreakdown = fnCurve.isBreakdown(0)
    print(f"Is breakdown: {isBreakdown}")
    ```

---

### **6. 类方法**
- **`className()`**
  - 返回类的名称（字符串）。
  - 返回值：`str`（"MFnAnimCurve"）。
  - 示例：
    ```python
    print(om.MFnAnimCurve.className())  # 输出：MFnAnimCurve
    ```

---

### **注意事项**
1. **模块导入**：使用 `MFnAnimCurve` 需要导入 `OpenMayaAnim` 模块：
   ```python
   import maya.api.OpenMayaAnim as om
   ```

2. **时间单位**：时间参数通常使用 `MTime` 对象，需注意 Maya 的时间单位（例如，帧或秒）。可以通过 `MTime.setUnit()` 设置时间单位。

3. **撤销支持**：许多修改方法支持 `MAnimCurveChange` 参数，用于实现撤销/重做功能。建议在需要撤销支持的场景中使用。

4. **切线类型**：切线类型（如 `kTangentLinear`、`kTangentSmooth`）会影响动画曲线的插值方式，需根据需求选择合适的类型。

5. **错误处理**：大多数方法返回 `MStatus` 或布尔值来指示操作是否成功，建议检查返回值以确保操作正确执行。

---

## 3. **动画曲线类型**
`MFnAnimCurve` 支持以下动画曲线类型（与 `MAnimCurve` 一致）：
- **`kAnimCurveTL`**: 平移曲线，输入为时间，输出为浮点值（通常表示距离，单位与场景线性单位相关）。
- **`kAnimCurveTA`**: 旋转曲线，输入为时间，输出为角度值（通常以弧度为单位）。
- **`kAnimCurveTU`**: 无单位曲线，输入为时间，输出为无单位值（例如缩放、透明度）。
- **`kAnimCurveTT`**: 时间到时间曲线，输入和输出均为时间（用于时间重映射）。
- **`kAnimCurveTAU`**: 单位到角度曲线。

通过 `animCurveType()` 方法可以确认曲线的类型。

---

## 4. **与 MAnimCurve 的区别**
虽然 `MFnAnimCurve` 和 `MAnimCurve` 都用于操作动画曲线，但它们有以下区别：
- **`MFnAnimCurve` 是功能集类**：
  - 它是 Maya API 中的功能集（Function Set），设计为操作特定类型的节点（动画曲线），通过绑定到 `MObject` 或 `MPlug` 工作。
  - 提供更高级的接口，方法名称和行为更符合 Maya 的功能集设计模式。
- **`MAnimCurve` 是独立类**：
  - `MAnimCurve` 是一个独立的对象类，直接表示动画曲线，操作更直接但功能较少。
  - 它不依赖于功能集机制，适合简单的关键帧操作。
- **功能差异**：
  - `MFnAnimCurve` 提供了更多高级功能，例如直接设置加权切线、处理 Breakdown 关键帧等。
  - `MAnimCurve` 的接口更简单，主要聚焦于基本的关键帧和切线操作。
- **使用场景**：
  - 使用 `MFnAnimCurve` 当需要更全面的动画曲线控制或与其他 Maya 功能集（如 `MFnDependencyNode`）结合使用。
  - 使用 `MAnimCurve` 当只需要快速操作关键帧或值，且不涉及复杂逻辑。

---

## 5. **使用场景**
`MFnAnimCurve` 适用于以下场景：
- **精确控制动画**：修改关键帧时间、值或切线，调整动画曲线形状。
- **动态生成动画**：通过 `addKey` 方法创建新的动画曲线。
- **分析动画数据**：使用 `evaluate` 方法获取特定时间的曲线值，或检查曲线类型和连接。
- **开发动画工具**：创建自定义工具，例如曲线平滑器、关键帧清理器或批量调整工具。
- **集成复杂工作流**：与其他 Maya API 功能集结合，操作复杂的动画场景。

---

## 6. **注意事项**
- **性能**：`MFnAnimCurve` 是低级 API，性能优于 `maya.cmds`，但代码复杂度较高。简单任务可以考虑使用 `maya.cmds`。
- **时间单位**：时间值通过 `MTime` 对象处理，需注意 Maya 的时间单位设置（帧或秒）。
- **错误处理**：确保输入的 `MObject` 或 `MPlug` 是有效的动画曲线节点，否则会抛出异常。
- **Undo 支持**：某些操作（如 `addKey` 或 `remove`）支持通过 `MAnimCurveChange` 对象实现撤销功能。
- **API 版本**：`MFnAnimCurve` 属于 Python API 2.0，需确保 Maya 版本兼容。

---

## 7. **示例代码**
以下是一个示例，展示如何使用 `MFnAnimCurve` 创建动画曲线、添加关键帧并评估值：
```python
from maya import OpenMaya, OpenMayaAnim

# 创建一个新的 animCurveTL 节点
dag_modifier = OpenMaya.MDagModifier()
anim_curve_obj = dag_modifier.createNode("animCurveTL")
dag_modifier.doIt()

# 初始化 MFnAnimCurve
anim_curve_fn = OpenMayaAnim.MFnAnimCurve(anim_curve_obj)

# 添加关键帧
anim_curve_fn.addKey(OpenMaya.MTime(0.0), 0.0, 
                     OpenMayaAnim.MFnAnimCurve.kTangentLinear, 
                     OpenMayaAnim.MFnAnimCurve.kTangentLinear)
anim_curve_fn.addKey(OpenMaya.MTime(10.0), 5.0, 
                     OpenMayaAnim.MFnAnimCurve.kTangentLinear, 
                     OpenMayaAnim.MFnAnimCurve.kTangentLinear)

# 评估曲线
time = OpenMaya.MTime(5.0)
value = OpenMaya.MScriptUtil().asDoublePtr()
anim_curve_fn.evaluate(time, value)
value = OpenMaya.MScriptUtil.getDouble(value)
print(f"Value at frame 5: {value}")

# 修改切线
anim_curve_fn.setInTangentType(0, OpenMayaAnim.MFnAnimCurve.kTangentSmooth)
anim_curve_fn.setIsWeighted(True)
```

---

## 8. **与 AnimCurveNode 的关系**
你之前提到的“AnimCurveNode”可能是一个误称或用户对 `MAnimCurve` 或 `MFnAnimCurve` 的非正式称呼。在 Maya Python API 2.0 中，动画曲线的核心类是 `MAnimCurve` 和 `MFnAnimCurve`，没有名为 `AnimCurveNode` 的类。如果你指的是其他插件或第三方库中的类，请提供更多上下文，我可以进一步确认。

---

## 9. **参考资料**
- **官方文档**：Autodesk Maya Python API 2.0 文档（`OpenMayaAnim.MFnAnimCurve` 部分）。
- **社区资源**：Maya 开发者论坛、GitHub 上的 Maya 脚本示例。
- **X 平台**：搜索 `#MayaPythonAPI` 或 `#MFnAnimCurve` 可获取最新的社区讨论和代码片段。

在 Autodesk Maya 的 Python API 2.0 中，`MFnAnimCurve` 是一个功能强大的类，用于操作动画曲线（Animation Curve）。它属于 `OpenMayaAnim` 模块，提供了对动画曲线的创建、修改和查询功能。以下是对 `MFnAnimCurve` 类中所有方法的详细介绍，基于 Maya 2025 的最新文档和功能集。

---
