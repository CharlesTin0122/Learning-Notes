# 概述
- 参见[11.0-pass](基础知识/11.0-pass.md)和[11.1-MFnAnimCurve](基础知识/11.1-MFnAnimCurve.md)
# 案例
## 基础功能应用
```python
import traceback
import maya.api.OpenMaya as om
import maya.api.OpenMayaAnim as oma


def create_anim_curve(obj: om.MObject, attr_name: str) -> oma.MFnAnimCurve:
    """
    根据对象和属性名称创建动画曲线.

    Args:
        obj (om.MObject): 要创建动画曲线的对象.
        attr_name (str): 要创建动画曲线的属性.

    Returns:
        oma.MFnAnimCurve: 创建的动画曲线函数集.
    """
    plug = find_plug(obj, attr_name)
    if not plug or plug.isConnected:
        return None

    anim_curve_fn = oma.MFnAnimCurve()
    try:
        # 创建动画曲线，未指定曲线类型，由maya决定其类型
        anim_curve_fn.create(plug, oma.MFnAnimCurve.kAnimCurveUnknown)
    except Exception:
        traceback.print_exc()
        return None

    return anim_curve_fn


def insert_key(obj: om.MObject, attr_name: str) -> int | None:
    """为动画曲线插入帧"""
    current_time = get_current_time()  # 获取当前时间栏时间
    anim_curve_fn = get_anim_curve_fn(obj, attr_name)  # 获取动画曲线函数集
    # 如果未获取到函数集则创建一条曲线，如果曲线创建失败返回None
    if not anim_curve_fn:
        anim_curve_fn = create_anim_curve(obj, attr_name)
        if not anim_curve_fn:
            return None
    # 获取当前时间动画曲线上的帧索引
    index = anim_curve_fn.find(current_time)
    # 如果当前时间没有帧，则插入帧（保持动画曲线形状）
    # 不能使用if not index,因为0也可能是帧索引
    if index is None:
        # insertKey(time, breakdown=False, change=None) ->int
        # 插入帧并保持曲线形状
        return anim_curve_fn.insertKey(current_time)
    # 其余情况 返回None
    return None


def remove_key(obj: om.MObject, attr_name: str) -> None:
    """移除当前时间关键帧"""
    anim_curve_fn = get_anim_curve_fn(obj, attr_name)
    if not anim_curve_fn:
        return
    index = anim_curve_fn.find(get_current_time())
    # 不能使用 if index:,因为0也可能是帧索引
    if not index is None:
        anim_curve_fn.remove(index)


def set_key_value(anim_curve_fn: oma.MFnAnimCurve, index: int, value: float) -> None:
    """设置关键帧的值"""
    anim_curve_fn.setValue(index, value)


def set_key_time(anim_curve_fn: oma.MFnAnimCurve, index: int, time: om.MTime) -> None:
    """设置关键帧的时间, setInput而不是setTime
    注意：设置帧时间不能超过前后帧的范围，否则会导致index失效
    如果确实要修改时间超过前后帧的范围，可以先删除帧，然后在新位置重新插入
    """
    anim_curve_fn.setInput(index, time)


def display_anim_curve_info(anim_curve_fn: oma.MFnAnimCurve) -> None:
    """打印动画曲线信息"""
    print("动画曲线信息:")
    print(f"动画曲线类型: {anim_curve_fn.animCurveType}")
    print(f"帧总数: {anim_curve_fn.numKeys}")
    print(f"是否静态曲线：{anim_curve_fn.isStatic}")
    print(f"前无限类型： {anim_curve_fn.preInfinityType}")
    print(f"后无限类型： {anim_curve_fn.postInfinityType}")


def display_key_info(anim_curve_fn: oma.MFnAnimCurve, index: int) -> None:
    """打印帧信息"""
    print(f"frame index：{index}")
    print(f"frame time：{anim_curve_fn.input(index).value}")
    print(f"frame value：{anim_curve_fn.value(index)}")
    print(f"breakdown：{anim_curve_fn.isBreakdown(index)}")
    print(f"tangents_locked：{anim_curve_fn.tangentsLocked(index)}")
    print(f"inTangents_type：{anim_curve_fn.inTangentType(index)}")
    print(f"inTangebts_value：{anim_curve_fn.getTangentXY(index, True)}")
    print(f"outTangents_type：{anim_curve_fn.outTangentType(index)}")
    print(f"outTangebts_value：{anim_curve_fn.getTangentXY(index, False)}")


"""-------------------------------------辅助函数-----------------------------------------"""


def get_anim_curve_fn(obj: om.MObject, attr_name: str) -> oma.MFnAnimCurve:
    """
    根据对象和属性名称获取其对应动画曲线的函数集

    Args:
        obj (om.MObject): 要查找函数集的对象.
        attr_name (str): 要查找函数集的属性名称.

    Returns:
        oma.MFnAnimCurve or None: 如果找到返回函数集，如果没有找到返回None
    """
    plug = find_plug(obj, attr_name)  # 找到插头
    if not plug:
        return None
    source_plug = plug.source()  # 获取插头的源插头
    source_node = source_plug.node()  # 获取 MPlug 的 MObject
    anim_curve_fn = oma.MFnAnimCurve()  # 创建动画曲线函数集
    # 如果对象和函数集对应，则设置函数集对象然后返回函数集
    if anim_curve_fn.hasObj(source_node):
        anim_curve_fn.setObject(source_node)
        return anim_curve_fn
    return None


def get_current_time() -> om.MTime:
    """获取当前时间"""
    return oma.MAnimControl.currentTime()


def find_plug(obj: om.MObject, attr_name: str) -> om.MPlug:
    """
    根据对象和属性名称找到插头
    Args:
        obj (om.MObject): 要查找插头的对象.
        attr_name (str): 属性的名称.
    Returns:
        om.MPlug: 如果找到返回插头，如果没有找到返回None
    """
    depend_fn = om.MFnDependencyNode(obj)  # 获取对象的DependNode函数集
    try:
        plug = depend_fn.findPlug(attr_name, False)  # 查找插头
    except Exception:
        om.MGlobal.displayWarning(
            f"Attribute not found: {depend_fn.name()}.{attr_name}"
        )
        return None
    return plug  # 返回插头


if __name__ == "__main__":
    # 获取选择列表
    selection = om.MGlobal.getActiveSelectionList()
    # 获取选择列表第一项
    if selection.length() > 0:
        obj = selection.getDependNode(0)
        anim_curve_fn = get_anim_curve_fn(obj, "translateZ")
        if anim_curve_fn:
            for i in range(anim_curve_fn.numKeys):
                display_key_info(anim_curve_fn, i)

    else:
        om.MGlobal.displayError("An object is not selected")

```
## 动画曲线修改命令
```python
import maya.api.OpenMaya as om
import maya.api.OpenMayaAnim as oma
import pymel.core as pm


def maya_useNewAPI():
    """告诉maya调用的是maya Python API 2.0"""
    pass


class AnimCurveCmd(om.MPxCommand):
    """自定义动画曲线命令
    pm.animCurveCmd(cc=True)：创建动画曲线
    pm.animCurveCmd(set=True)：设置关键帧
    pm.animCurveCmd(rem=True)：移除关键帧
    pm.animCurveCmd(dc=True)：删除动画曲线
    """

    COMMAND_NAME = "animCurveCmd"

    CREATE_CURVE_FLAG = ["-cc", "-createCurve"]
    DELETE_CURVE_FLAG = ["-dc", "-deleteCurve"]
    SET_KEY_FLAG = ["-set", "-setKey"]
    REMOVE_KEY_FLAG = ["-rem", "-removeKey"]

    def __init__(self):
        super().__init__()
        self.dg_modifier = om.MDGModifier()  # DG修改器
        self.dg_changed = False

        self.anim_curve_change = oma.MAnimCurveChange()  # 动画曲线改变栈
        self.anim_modified = False

    def doIt(self, arg_list):
        # 解析参数列表
        try:
            arg_db = om.MArgDatabase(self.syntax(), arg_list)
        except Exception:
            self.displayError("Invalid arguments")
            raise
        # 获取参数列表中的对象列表
        selection_list = arg_db.getObjectList()
        if selection_list.length() == 0:
            raise RuntimeError("No object selected")
        self.selected_obj = selection_list.getDependNode(0)
        if self.selected_obj.apiType() != om.MFn.kTransform:
            raise RuntimeError("Selected object is not a transform node")
        # 标志是否被使用
        create_curve_flag = arg_db.isFlagSet(AnimCurveCmd.CREATE_CURVE_FLAG[0])
        delete_curve_flag = arg_db.isFlagSet(AnimCurveCmd.DELETE_CURVE_FLAG[0])
        set_key_flag = arg_db.isFlagSet(AnimCurveCmd.SET_KEY_FLAG[0])
        remove_key_flag = arg_db.isFlagSet(AnimCurveCmd.REMOVE_KEY_FLAG[0])
        # 要修改的属性
        attrs = ["tx", "ty", "tz"]

        # 命令逻辑
        if create_curve_flag:
            if not self.create_anim_curves(self.selected_obj, attrs):
                raise RuntimeError("No anim curves were created")
        elif delete_curve_flag:
            if not self.delete_anim_curves(self.selected_obj, attrs):
                raise RuntimeError("No anim curves were deleted")
        elif set_key_flag:
            if not self.set_key(self.selected_obj, attrs):
                raise RuntimeError("No keys were set")
        elif remove_key_flag:
            if not self.remove_key(self.selected_obj, attrs):
                raise RuntimeError("No keys were removed")
        else:
            raise RuntimeError("A valid flag is required")

    def isUndoable(self):
        return True

    def undoIt(self):
        """撤销逻辑"""
        if self.dg_changed:
            self.dg_modifier.undoIt()
        if self.anim_modified:
            self.anim_curve_change.undoIt()

    def redoIt(self):
        """重做逻辑"""
        if self.dg_changed:
            self.dg_modifier.doIt()
        if self.anim_modified:
            self.anim_curve_change.redoIt()

    def create_anim_curves(self, obj: om.MObject, attrs: list) -> bool:
        """创建动画曲线"""
        for attr in attrs:
            plug = self.get_attr_plug(obj, attr)
            if not plug or plug.isConnected:
                continue
            anim_curve_fn = oma.MFnAnimCurve()
            try:
                """创建动画曲线，参数：
                * plug：要创建曲线的插头
                * animCurveType：曲线类型
                * modifier：储存操作栈的修改器MDGModifier，用于撤销和重做
                """
                anim_curve_fn.create(
                    plug, oma.MFnAnimCurve.kAnimCurveTL, self.dg_modifier
                )
                self.dg_changed = True
            except Exception:
                om.MGlobal.displayWarning(f"Failed to create anim curve: {obj}.{attr}")
        return self.dg_changed

    def delete_anim_curves(self, obj: om.MObject, attrs: list) -> bool:
        """删除动画曲线节点"""
        for attr in attrs:
            # 通过函数集获取动画曲线节点
            anim_curve_fn = self.get_anim_curve_fn(obj, attr)
            anim_curve_node = anim_curve_fn.object()
            if not anim_curve_node:
                continue
            # 通过DG修改器删除节点
            self.dg_modifier.deleteNode(anim_curve_node)
            self.dg_changed = True
        if self.dg_changed:
            # 删除节点
            self.dg_modifier.doIt()
        return self.dg_changed

    def set_key(self, obj: om.MObject, attrs: list) -> bool:
        """为属性设置关键帧"""
        # 获取时间栏当前时间
        current_time = self.get_current_time()
        # 遍历属性列表
        for attr in attrs:
            value = self.get_attr_val(obj, attr)  # 获取当前属性值
            if value is None:
                continue
            anim_curve_fn = self.get_anim_curve_fn(obj, attr)  # 获取函数集
            if not anim_curve_fn:
                continue
            index = anim_curve_fn.find(current_time)  # 获取当前时间栏帧索引
            # 如果当前时间没有帧，则添加关键帧
            if index is None:
                """添加关键帧，参数：
                * time(MTime):添加关键帧的时间
                * value（float）：添加关键帧的值
                * change（MAnimCurveChange）：储存操作栈的修改器MAnimCurveChange，用于撤销和重做
                """
                anim_curve_fn.addKey(current_time, value, change=self.anim_curve_change)
            # 如果当前时间有帧，则设置帧的值
            else:
                anim_curve_fn.setValue(index, value, change=self.anim_curve_change)
            # 设置动画修改布尔值为真
            self.anim_modified = True
        # 返回动画修改布尔值
        return self.anim_modified

    def remove_key(self, obj: om.MObject, attrs: list):
        """移除关键帧"""
        current_time = self.get_current_time()  # 获取当前时间
        for attr in attrs:
            anim_curve_fn = self.get_anim_curve_fn(obj, attr)  # 获取函数集
            if not anim_curve_fn:
                continue
            index = anim_curve_fn.find(current_time)  # 通过时间找到帧索引
            if index is not None:
                # 移除关键帧
                anim_curve_fn.remove(index, change=self.anim_curve_change)
                # 设置动画修改布尔值为真
                self.anim_modified = True
        # 返回动画修改布尔值
        return self.anim_modified

    """----------------------------辅助函数------------------------------"""

    def get_attr_plug(self, obj: om.MObject, attr: str) -> om.MPlug | None:
        """查找对象的属性插头"""
        depend_fn = om.MFnDependencyNode(obj)
        try:
            plug = depend_fn.findPlug(attr, False)
        except Exception:
            om.MGlobal.displayWarning(f"Attribute not found: {depend_fn.name()}.{attr}")
            return None
        return plug

    def get_anim_curve_fn(self, obj: om.MObject, attr: str) -> oma.MFnAnimCurve | None:
        """根据对象和属性获取动画曲线函数集"""
        plug = self.get_attr_plug(obj, attr)
        if not plug:
            return None
        source_plug = plug.source()
        anim_curve_fn = oma.MFnAnimCurve()

        if anim_curve_fn.hasObj(source_plug.node()):
            anim_curve_fn.setObject(source_plug.node())
            return anim_curve_fn
        return None

    def get_current_time(self) -> om.MTime:
        """获取当前时间栏时间"""
        return oma.MAnimControl.currentTime()

    def get_attr_val(self, obj: om.MObject, attr: str) -> float | None:
        """根据对象和属性获取属性值"""
        plug = self.get_attr_plug(obj, attr)
        if plug:
            return plug.asDouble()
        return None

    @classmethod
    def create_syntax(cls):
        syntax = om.MSyntax()

        syntax.addFlag(*cls.CREATE_CURVE_FLAG)
        syntax.addFlag(*cls.DELETE_CURVE_FLAG)
        syntax.addFlag(*cls.SET_KEY_FLAG)
        syntax.addFlag(*cls.REMOVE_KEY_FLAG)
        # 选择列表作为参数，可使用场景内的对象名称作为参数
        # 参数1：选择列表，参数2：场景内选择的最小数量，参数3：场景内选择的最大数量
        syntax.setObjectType(om.MSyntax.kSelectionList, 1, 1)
        # 使用maya场景内的选择列表作为默认参数
        syntax.useSelectionAsDefault(True)

        return syntax

    @classmethod
    def creator(cls):
        return cls()


def initializePlugin(plugin):
    """加载插件并注册命令"""
    vendor = "Charles Tian"
    version = "1.0.0"

    plugin_fn = om.MFnPlugin(plugin, vendor, version)
    try:
        plugin_fn.registerCommand(
            AnimCurveCmd.COMMAND_NAME, AnimCurveCmd.creator, AnimCurveCmd.create_syntax
        )
    except Exception as e:
        om.MGlobal.displayError(
            f"Failed to register command: {AnimCurveCmd.COMMAND_NAME}, {e}"
        )


def uninitializePlugin(plugin):
    """卸载插件，并取消注册命令"""
    plugin_fn = om.MFnPlugin(plugin)
    try:
        plugin_fn.deregisterCommand(AnimCurveCmd.COMMAND_NAME)
    except Exception as e:
        om.MGlobal.displayError(
            f"Failed to deregister command: {AnimCurveCmd.COMMAND_NAME}, {e}"
        )


if __name__ == "__main__":
    pm.newFile(force=True)
    pm.polyCube()

    plugin_name = "anim_curve_cmd"
    pm.unloadPlugin(plugin_name)
    pm.loadPlugin(plugin_name)

    pm.animCurveCmd(cc=True)
    pm.animCurveCmd(set=True)
    pm.animCurveCmd(rem=True)
    pm.animCurveCmd(dc=True)

```