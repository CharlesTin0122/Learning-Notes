DynamicSingleDelegateActor.h
```cpp


#pragma once

#include "CoreMinimal.h"
#include "GameFramework/Actor.h"
#include "DynamicSingleDelegateActor.generated.h"

//动态单播委托
DECLARE_DYNAMIC_DELEGATE_TwoParams(FMyDynamicSingleDelegete, FString, InName, int32, InMoney);
//带返回值动态单播委托
DECLARE_DYNAMIC_DELEGATE_RetVal_OneParam(int32, FMyRetDynamicSingleDelegete, FString, InName);

UCLASS()
class QUICKSTART_API ADynamicSingleDelegateActor : public AActor
{
	GENERATED_BODY()
	
public:	
	// Sets default values for this actor's properties
	ADynamicSingleDelegateActor();

protected:
	// Called when the game starts or when spawned
	virtual void BeginPlay() override;

public:	
	// Called every frame
	virtual void Tick(float DeltaTime) override;


protected:
	//动态委托实例化
	FMyDynamicSingleDelegete DynamicSingleDelegete;
	FMyRetDynamicSingleDelegete RetDynamicSingleDelegete;
public:
	//初始化动态单播委托
	UFUNCTION(BlueprintCallable)
	void InitDynamicDelegate(FMyDynamicSingleDelegete MyDelegate);
	//释放动态单播委托
	UFUNCTION(BlueprintCallable)
	void ReleaseDynamicDelegate();
	//执行动态单播委托
	UFUNCTION(BlueprintCallable)
	void CallDynamicDelegate(FString InName, int32 InMoney);

	//初始化带返回值动态单播委托
	UFUNCTION(BlueprintCallable)
	void InitRetDynamicDelegate(FMyRetDynamicSingleDelegete MyDelegate);
	//释放带返回值动态单播委托
	UFUNCTION(BlueprintCallable)
	void ReleaseRetDynamicDelegate();
	//执行带返回值动态单播委托
	UFUNCTION(BlueprintCallable)
	void CallRetDynamicDelegate(FString InName);
	
};

```
DynamicSingleDelegateActor.cpp
```cpp



#include "DynamicSingleDelegateActor.h"


// Sets default values
ADynamicSingleDelegateActor::ADynamicSingleDelegateActor()
{
 	// Set this actor to call Tick() every frame.  You can turn this off to improve performance if you don't need it.
	PrimaryActorTick.bCanEverTick = true;

}

// Called when the game starts or when spawned
void ADynamicSingleDelegateActor::BeginPlay()
{
	Super::BeginPlay();
	
}

// Called every frame
void ADynamicSingleDelegateActor::Tick(float DeltaTime)
{
	Super::Tick(DeltaTime);

}

void ADynamicSingleDelegateActor::InitDynamicDelegate(FMyDynamicSingleDelegete MyDelegate)
{
	DynamicSingleDelegete = MyDelegate;
}

void ADynamicSingleDelegateActor::ReleaseDynamicDelegate()
{
	DynamicSingleDelegete.Clear();
}

void ADynamicSingleDelegateActor::CallDynamicDelegate(FString InName, int32 InMoney)
{
	DynamicSingleDelegete.ExecuteIfBound(InName, InMoney);
}


void ADynamicSingleDelegateActor::InitRetDynamicDelegate(FMyRetDynamicSingleDelegete MyDelegate)
{
	RetDynamicSingleDelegete = MyDelegate;
}

void ADynamicSingleDelegateActor::ReleaseRetDynamicDelegate()
{
	RetDynamicSingleDelegete.Clear();
}

void ADynamicSingleDelegateActor::CallRetDynamicDelegate(FString InName)
{
	RetDynamicSingleDelegete.Execute(InName);
}



```